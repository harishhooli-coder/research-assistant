# Research Assistant - Python backend (pinned, current/stable versions)

# --- LangGraph agent + LLM + tools ---
langgraph==1.2.6
langgraph-prebuilt==1.1.0
langgraph-checkpoint==4.1.1
langchain==1.3.11
langchain-core==1.4.8
langchain-anthropic==1.4.8
langchain-openai==1.3.5
anthropic==0.112.0
tavily-python==0.7.26
httpx==0.28.1
trafilatura==2.1.0
tiktoken==0.13.0

# --- Telegram bot (Phase 1) ---
python-telegram-bot==22.8

# --- FastAPI backend + arq queue + SSE (Phase 3) ---
fastapi==0.138.1
uvicorn[standard]==0.49.0
arq==0.28.0
sse-starlette==3.4.5

# --- Database: SQLAlchemy 2.0 async + asyncpg + Alembic (Neon Postgres) ---
SQLAlchemy==2.0.51
asyncpg==0.31.0
alembic==1.18.5
greenlet==3.5.3

# --- Redis (arq queue + pub/sub + episodic memory) ---
redis==5.3.1

# --- Config ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- Testing (offline: no real keys / network) ---
pytest==9.1.1
pytest-asyncio==1.4.0
anyio==4.14.1
fakeredis==2.36.2
aiosqlite==0.22.1
